// Package buildinfo хранит общие для sshmon сборочные константы —
// единый источник версии для флага --version и MCP serverInfo.
package buildinfo

// Version — версия релиза sshmon.
const Version = "0.4.0"
